---
# Usage:
#   ansible-playbook playbooks/infra_create.yml
#   ansible-playbook playbooks/infra_create.yml -e provider=aws
# Requires: group_vars/all.yml + client.yml + server.yml
- name: Create infrastructure (Terraform)
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/client.yml
    - ../group_vars/server.yml
  roles:
    - role: terraform
      terraform_action: create

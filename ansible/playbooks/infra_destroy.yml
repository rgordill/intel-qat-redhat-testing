---
# Usage:
#   ansible-playbook playbooks/infra_destroy.yml
#   ansible-playbook playbooks/infra_destroy.yml -e provider=aws
# Requires: group_vars/all.yml + client.yml + server.yml
- name: Destroy infrastructure (Terraform)
  hosts: localhost
  connection: local
  gather_facts: false
  become: false
  vars_files:
    - ../group_vars/all.yml
    - ../group_vars/client.yml
    - ../group_vars/server.yml
  roles:
    - role: terraform
      terraform_action: destroy

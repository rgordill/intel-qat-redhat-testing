---
# Usage: ansible-playbook playbooks/terraform_aws.yml -e aws_region=eu-west-1
- name: Provision AWS instances (Terraform)
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - ../group_vars/all.yml
  roles:
    - role: terraform_aws

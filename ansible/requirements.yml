---
# Pin for reproducible installs (ansible-galaxy collection install -r requirements.yml).
# Last resolved: ansible.posix 1.6.2, cloud.terraform 4.0.0, community.crypto 2.26.7, community.general 9.5.0
collections:
  - name: ansible.posix
    version: "1.6.2"
  - name: cloud.terraform
    version: "4.0.0"
  - name: community.crypto
    version: "2.26.7"
  - name: community.general
    version: "9.5.0"

---
# Enable registration before any dnf installs on unsubscribed RHEL guests (libvirt/AWS RHEL).
# rhel_org_id / rhel_activation_key come from group_vars/all.yml (vault_rhel_* in vault.yml).
rhel_subscription_enabled: false
